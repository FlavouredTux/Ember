{
  "id": "ember.roblox-demo",
  "name": "Roblox Markers (demo)",
  "version": "0.2.0",
  "description": "Demo plugin: matchers + panels. Finds every RBX::Logger reference and offers an inspection panel + optional note proposals.",
  "apiVersion": 1,
  "entry": "main.cjs",
  "matchers": [
    { "kind": "string-present", "text": "RBX::Logger" }
  ],
  "permissions": [
    "read.binary-summary",
    "read.strings",
    "read.annotations",
    "project.note"
  ],
  "contributes": {
    "panels": [
      {
        "id": "logger-sites",
        "title": "RBX::Logger sites",
        "description": "Every function that references the RBX::Logger string, with its defining xref.",
        "command": "list-logger-sites"
      }
    ]
  }
}
