// cleanup_on_fail
u64 cleanup_on_fail(u64 a1) {
  u64 local_10;
  void* r_malloc = malloc(0x40);
  local_10 = r_malloc;
  if (r_malloc) {
    if (!a1) {
      free(local_10);
      return 0xffffffff;
    }
    u64 r_strlen = strlen(a1);
    if (r_strlen > 0x20) {
      free(local_10);
      return 0xffffffff;
    }
    strcpy(local_10, a1);
    free(local_10);
    return 0;
  }
  return 0xffffffff;
}
