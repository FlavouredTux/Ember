// main
void main(u64 a1, u64 a2) {
  u32 t2 = (u32)a1;
  one_arg();
  u32 t13 = (t2 - 0x1);
  if (((i64)t2 > (i64)0x1)) {
    fprintf(stderr, "a=%s b=%s\n", a2[0], a2[1]);
  }
  no_args();
  printf("%*d\n", (u32)a1, (u64)g_count);
  return (u64)g_count;
}
