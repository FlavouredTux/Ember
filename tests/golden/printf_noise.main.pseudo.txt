// main
u32 main(u32 a1, u64* a2) {
  u32 t2 = a1;
  u64 r_strlen = strlen(a2[0]);
  printf("len=%zu\\n", r_strlen);
  if ((i64)t2 > 1) {
    fprintf(stderr, "a=%s b=%s\\n", a2[0], a2[1]);
  }
  fwrite("hello\\n", 1, 6, stdout);
  puts("ready");
  printf("%*d\\n", (u32)a1, (u64)g_count);
  return g_count;
}
