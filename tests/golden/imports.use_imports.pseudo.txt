// use_imports
u64 use_imports(char* a1, u64 a2) {
  if (a1) {
    u64 r_strlen = strlen(a1);
    g_count += r_strlen;
    puts(a1);
  }
  if (a2) {
    fputs(a2, stdout);
  }
  char* r_getenv = getenv("HOME");
  if (r_getenv) {
    g_count++;
    return g_count + 1;
  }
  return r_getenv;
}
