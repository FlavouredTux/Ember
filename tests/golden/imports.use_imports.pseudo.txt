// use_imports
void use_imports(u64 a1, u64 a2) {
  if ((rdi != 0x0)) {
    local_10 = rsi;
    local_18 = rdi;
    u64 r_strlen = strlen(rdi);
    g_count = (r_strlen + g_count);
    puts(local_18);
  }
  if ((rdx != 0x0)) {
    fputs(rdx, stdout);
  }
  u64 r_getenv = getenv("HOME");
  if ((r_getenv != 0x0)) {
    u64 t28 = (g_count + 0x1);
    g_count = t28;
  }
  return rax;
}
