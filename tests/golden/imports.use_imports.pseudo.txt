// use_imports
u64 use_imports(char* a1, char* a2) {
  if (a1) {
    u64 r_strlen = strlen(a1);
    g_count += r_strlen;
    puts(a1);
  }
  if (a2) {
    fputs(a2, stdout);
  }
  u64 r_getenv = getenv("HOME");
  if (r_getenv) {
    g_count++;
  }
  return rax_10;
}
