// shared_tail
u64 shared_tail(u64 a1) {
  local_20 = a1;
  local_c = 0;
  if (a1) {
    if (*(u8*)(local_20)) {
      u64 r_strlen = strlen(local_20);
      local_c = (u32)r_strlen;
    }
  }
  g_log = local_c;
  return local_c;
}
