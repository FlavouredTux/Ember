// multi_exit
u64 multi_exit(u64 a1, u64 a2) {
  if (!a1) {
    return -1;
  }
  if (!a2) {
    return -1;
  }
  u64 r_strlen = strlen(a1);
  u64 r_strlen_2 = strlen(a2);
  if (r_strlen != r_strlen_2) {
    return -1;
  }
  return 0;
}
